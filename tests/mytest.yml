version: 2

models:
  - name: DATA3
    columns:
      - name: CUSTOMER_ID
        tests: [unique, not_null]

  - name: dim_customer
    columns:
      - name: CUSTOMER_ID
        tests: [unique, not_null]

  - name: test_unique
    columns:
      - name: id
        tests: [unique]

  - name: tgt_tbl_data
    columns:
      - name: gender
        tests:
          - accepted_values:
              values: ['M','F']
